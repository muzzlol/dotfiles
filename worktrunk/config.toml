[commit-generation]
args = []
command = ""

[projects."github.com/muzzlol/spectra"]
approved-commands = [
    "bun install",
    "cp {{ repo_path }}/.env.local {{ worktree_path }}/.env.local",
    "chmod +x {{ worktree_path }}/scripts/*.sh",
]
